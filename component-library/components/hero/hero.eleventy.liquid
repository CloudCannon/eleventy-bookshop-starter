<div class="c-title">
    {% for tag in tags %}
        {% bookshop "tag" bind: tag %}
    {% endfor %}
    <h1 class="c-title__title">
        {{ title }}
    </h1>
    {% if content %}
        {% bookshop "content" bind: content %}
    {% endif %}
</div>